This directory attempts to do the marching cubes using pthreads

first, go to the build directory (it's in the PolyDataToImageData directory), 
and do the sudo cmake .. and sudo make stuff

then, to run, what we do is:

sudo ./ApplyingVtkMarchingCubes 8 AllStars.vtk

pretty chill
